<MudPaper Class="py-3" Square="true">
    <MudNavMenu Dense="true" Color="Color.Info">
        <AuthorizeView>
            <Authorized>
                <MudText Typo="Typo.h6" Class="px-4">@context.User.Identity?.Name</MudText>
                <MudText Typo="Typo.body2" Class="px-4 mud-text-secondary">Authorized</MudText>
                <MudDivider Class="my-2"/>
            </Authorized>
        </AuthorizeView>
        <MudNavLink Href="log/messages" Icon="@Icons.Material.Outlined.Person" IconColor="Color.Inherit" Match="NavLinkMatch.All">Message logs</MudNavLink>
    </MudNavMenu>
</MudPaper>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}